# - Config file for the face_seg package
# It defines the following variables
#  face_seg_INCLUDE_DIRS - include directories for face_seg
#  face_seg_LIBRARIES    - libraries to link against
#  face_seg_EXECUTABLE   - the face_seg executable
 
# Calculated paths
get_filename_component(face_seg_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" DIRECTORY)
set(face_seg_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/include")

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET face_seg AND NOT face_seg_BINARY_DIR)
  include("${face_seg_CMAKE_DIR}/face_seg-targets.cmake")
endif()
 
# These are IMPORTED targets created by face_seg-targets.cmake
set(face_seg_LIBRARIES face_seg)
if(TARGET face_seg_images)
	set(face_seg_EXECUTABLE face_seg_images)
endif()